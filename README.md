# Anime Track (minimal)

Requirements:
- Python 3.9+
- Flask

Install:
$ python -m venv venv
$ . venv/bin/activate
$ pip install Flask

Initialize DB:
$ python scripts/init_db.py

Run:
$ python run.py

Open http://127.0.0.1:5000

